import 'package:flutter/material.dart';

/// An abstract class that serves as the base for all template widgets.
///
/// Template widgets define the common layout structure for different pages
/// within the application. Extend this class to create custom templates
/// with predefined layouts.
abstract class Template extends StatelessWidget {
  const Template({super.key});
}
